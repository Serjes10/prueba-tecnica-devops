controller:

  #Exposicion de JENKINS para abrirlo desde Windows
  serviceType: NodePort
  nodePort: 30080


  installPlugins:
    - kubernetes:latest
    - workflow-aggregator:latest
    - git:latest
    - configuration-as-code:latest
    - docker-workflow:latest

  
  admin:
    username: admin
    password: admin-devops

resources:
  requests: 
    cpu: "500m"
    memory: "512Mi"